{
  "name": "downloader-anything",
  "version": "1.0.0",
  "description": "Simple video/post/music downloader using yt-dlp - Cloudflare Pages + Workers",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:pages": "wrangler pages deploy frontend --project-name=downloader-anything",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "hono": "^4.7.5"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
